# Configs

Reusable configuration files and config-loading helpers belong here.

Suggested layout:

```text
configs/
  envs/
  agents/
  algorithms/
  experiments/
```

Prefer YAML for experiment settings so runs can be reproduced.
